/// [Enum] type for medicine's `name_type`.
///
/// `b` is for Branded, and `g` is for Generic.
enum NameType {
  b,
  g,
}

/// Custom Medicine related constants.
class MedicineConstants {
  /// `medicineList`
  static const String medicineList = "medicineList";

  /// `scroll`
  static const String scroll = "scroll";
}
